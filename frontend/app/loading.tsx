export default function Loading() {
    return (
        <main>
            <h2>Loading CivicFlow reports...</h2>
            <p>Please wait while we fetch the latest reports.</p>
        </main>
    );
}